# 프로젝트 진행 체크리스트

> 기준 문서: `docs/project_idea_naming.md`
> 최종 업데이트: 2026-06-16

---

## 1단계 — 데이터 수집 및 구조화

- [x] Unihan TXT 파싱 (kKangXi 획수 기준) → `data/processed/unihan_maping/`
- [x] 자원오행 XLSX + peoplehanja.json 병합 → `data/processed/hanja_documents.json`
- [x] 81suri.json, yinyang.json → `data/processed/suri_documents.json`, `ohaeng_documents.json`
- [x] 가족관계의 등록 등에 관한 법률 PDF 수집 → `data/raw/pdf/`
- [x] 가족관계의 등록 등에 관한 규칙 PDF 수집 → `data/raw/pdf/`
- [x] 순우리말 이름 크롤링 (baby-name.kr 1~11페이지) → `data/processed/urimalsam_names.json` (301개)
- [x] 이름 빈도 통계 → `data/raw/reference/2016_2026상위_출생신고_이름_현황.xls` (API 대신 XLS 사용)
- [x] 국가법령정보 API 키 발급 완료 (`LAW_API_KEY`)
- [x] 우리말샘 API 키 발급 완료 (`URIMALSAM_API_KEY`)

---

## 2단계 — 전처리

- [x] 법령 PDF KoNLPy Okt 전처리 → `data/processed/law_articles.json` (`preprocess_law.py`)
- ~~청킹 전략 비교 실험~~ — 모든 데이터가 레코드 단위로 구조화되어 있어 불필요
- ~~순우리말 이름 KoNLPy 전처리~~ — 크롤링 데이터가 이미 정제된 구조화 데이터이므로 불필요
- ~~SQLite 한자 수치 DB~~ — ChromaDB로 대체 완료, 미사용

---

## 3단계 — 인덱싱

### ChromaDB (완료)

- [x] `hanja_col` — 2,420건
- [x] `suri_col` — 81건
- [x] `ohaeng_col` — 125건
- [x] `law_col` — 248건
- [x] `urimalsam_col` — 301건
- [x] `paper_col` — 266건 (표 37 + 본문 229)

### 기타

- [x] Neo4j 스키마 설계 및 인덱싱 완료

---

## 4단계 — LangGraph StateGraph (`src/graph/naming_graph.py`)

- [x] StateGraph 뼈대 (Router + 6방향 분기 + generate 노드)
- [x] Router LLM 직접 선택 로직 (키워드 기반 → LLM JSON 분기 전환)
- [x] generate 노드 — gpt-5.4-mini 연결
- [x] `internal_rag_node` → `rag_server.search_rag()` 실제 연결
- [x] `sql_db_node` → `db_server` 함수 실제 연결
- [x] `external_api_node` → `law_server` 함수 실제 연결
- [x] `graph_db_node` → Neo4j 연결 완료
- [x] ReAct 루프 구현 완료 (MAX_ITERATIONS=5, 6방향 분기)
- [x] 출처 라벨 포함 답변 형식 (`[한자: 자원오행표 木오행]` 형식)
- [x] 면책 고지 문구 추가 완료

---

## 5단계 — MCP 서버

- [x] `rag_server.py` — `search_rag`, `list_collections` Tool 완성 (`urimalsam_col`, `paper_col` 포함)
- [x] `db_server.py` — `calculate_name_suri`, `find_lucky_strokes`, `lookup_ohaeng_combo`, `search_name_stats` Tool 완성
- [x] `law_server.py` — `search_law`, `get_law_article`, `verify_korean_word` Tool 완성
- [x] `graph_server.py` — `check_graph_status`, `lookup_hanja`, `check_person_name_hanja`, `get_ohaeng_relations`, `recommend_hanja_by_ohaeng`, `answer_graph_query` Tool 완성

---

## 6단계 — LLM 답변 생성

- [x] gpt-5.4-mini (`ChatOpenAI`) 연결
- [x] 시스템 프롬프트 완성
- [x] 출처 라벨 포함 답변 형식 완료
- [x] 면책 고지 문구 완료

---

## 7단계 — sLLM 파인튜닝 (병렬 진행)

- [x] 작명 조건 QA 데이터셋 구성 완료 (50건, CoT 방식)
- [ ] QLoRA 파인튜닝 (Qwen3.5:4b) — 🔄 진행 중

---

## 테스트 및 평가

- [x] Ground Truth QA 셋 구성 (30~50개)
- [x] LLM-as-a-Judge 평가 파이프라인 (Context Relevance / Groundedness / Answer Relevance)
- [x] BLEU / ROUGE 측정
- [ ] sLLM 파인튜닝 전후 LM-Eval 벤치마크 — 🔄 진행 중

---

## 현재 위치

> **전체 파이프라인 완성 (4~6단계 통합 완료) — 발표 당일(2026-06-17) 시연 준비 완료**
> 최종 업데이트: 2026-06-16
>
> 진행 중인 작업:
> 1. Qwen3.5:4b QLoRA 파인튜닝 (평가항목용) — 🔄 진행 중
> 2. LM-Eval 벤치마크 — 🔄 진행 중 (파인튜닝 완료 후 실행)
