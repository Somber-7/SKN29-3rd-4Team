# 🚀 작명 전문가 모델(Qwen3.5-4B) 파인튜닝 프로세스 정리

본 문서는 SKN29-3rd-4Team 프로젝트에서 진행한 **Qwen3.5-4B 모델의 파인튜닝(PEFT) 및 평가 과정**을 단계별로 기록한 문서입니다. 향후 프로젝트 보고서 작성 및 재현(Reproduce)을 위해 작성되었습니다.

---

## 1단계: 학습 데이터 준비 및 전처리
* **스크립트**: `src/data/append_bulk_data.py`
* **내용**: 
  * 파이프라인(GPT-5.4-mini)을 거쳐 생성된 초기 52개의 질의응답(QA) 데이터를 **약 300건(296건)**으로 대폭 증강(Data Augmentation)했습니다.
  * 중복된 데이터나 품질이 떨어지는 데이터를 필터링하고, 최종적으로 모델이 학습할 수 있는 `Instruction` - `Output` 형식의 JSON 파일(`finetune_data.json`)로 변환했습니다.

## 2단계: LoRA 파인튜닝 학습 수행 (train.py)
* **스크립트**: `train.py`
* **실행 환경**: RunPod (GPU: RTX 4000 Ada)
* **주요 최적화 기법 (엔지니어링 포인트)**:
  1. **Base 모델 튜닝**: Instruct 모델의 기존 대화 편향(Bias)과 토크나이저 충돌 에러를 해결하기 위해, 편견이 없는 Base 모델(`Qwen3.5-4B`)을 선택하여 백지상태에서 마크다운 작명 포맷을 완벽히 주입했습니다.
  2. **LoRA 파라미터 최적화**: 40억 개의 거대한 파라미터를 모두 학습시키는 대신, 핵심 모듈(`q_proj`, `v_proj`)에만 LoRA(`r=8`, `alpha=16`)를 적용해 학습 속도를 극대화했습니다.
  3. **메모리 보호 기법**: 최신 GPU 환경에 맞춰 `bfloat16` 데이터 타입을 사용하고, Gradient Clipping(`max_grad_norm=0.3`)을 적용해 학습 도중 모델이 붕괴되는(Exploding) 현상을 완벽히 차단했습니다.
* **학습 결과**: 3 Epoch 수행 결과 정답률(Accuracy) 85.6%, Loss 0.62 달성 완료.

## 3단계: 성능 평가 (LM-Eval 및 BLEU/ROUGE)
학습된 모델이 기존 상식을 잃어버리는 **'재앙적 망각(Catastrophic Forgetting)'**에 빠지지 않았는지, 그리고 정답과 얼마나 일치하게 대답하는지 두 가지 방향으로 검증했습니다.

### 3-1. 글로벌 벤치마크 평가 (ARC)
* **스크립트**: `eval.sh`
* **방법**: `lm-evaluation-harness` 라이브러리를 활용해 파인튜닝 전/후의 추론 능력을 비교 채점했습니다.
* **결과**: `ARC-Easy`, `ARC-Challenge` 등 모든 추론 지표에서 파인튜닝 전보다 점수가 상승(약 1~3%)하여, 재앙적 망각 없이 안정적인 학습이 진행되었음을 증명했습니다.

| 벤치마크 과제 | 지표 (Metric) | Base 모델 (튜닝 전) | QLoRA 모델 (튜닝 후) | 결과 |
| :--- | :--- | :--- | :--- | :--- |
| **ARC-Easy** | acc | 0.8136 | **0.8169** | 향상 (▲ 0.0033) |
| **ARC-Easy** | acc_norm | 0.7563 | **0.7748** | 향상 (▲ 0.0185) |
| **ARC-Challenge** | acc | 0.5145 | **0.5427** | 향상 (▲ 0.0282) |
| **ARC-Challenge** | acc_norm | 0.5444 | **0.5555** | 향상 (▲ 0.0111) |

### 3-2. 문장 생성 품질 평가 (Base 모델 기반 파인튜닝)
* **스크립트**: `tests/run_inference.py --mode evaluate`
* **방법**: HuggingFace의 `evaluate` 모듈을 활용하여, 정답(Ground Truth)과 파인튜닝 모델이 생성한 마크다운 추천 결과를 단어 및 문장 구조 단위로 채점했습니다.
* **특이사항**: 평가 및 테스트 스크립트가 여러 개로 파편화되어 있던 것을 `run_inference.py` 하나의 CLI 툴로 리팩토링(통합)하여 유지보수성을 극대화했습니다.
* **평가 결과**: RAG 파이프라인(외부 지식 검색)이 완벽히 배제된 **단독 모델(Zero-shot) 평가**임에도 불구하고 아래와 같은 우수한 생성 품질을 달성했습니다.
  * **ROUGE-1**: `0.4353` / **ROUGE-2**: `0.1199` / **ROUGE-L**: `0.4190`
  * **BLEU 스코어**: `0.2241`

### 3-3. Instruct 모델 파인튜닝 실험 및 고찰 (Base vs Instruct)
추가적으로 이미 대화형으로 튜닝된 세계 최고 수준의 연구소 모델(`vector-institute/Qwen3.5-4B-UnBias-Plus-SFT-Instruct`)을 동일한 300개의 데이터로 파인튜닝 해보는 비교 실험을 진행했습니다. 이 모델은 특수 훈련(SFT)을 통해 사회적 편견이 제거된(UnBias) 아주 정중하고 윤리적인 챗봇 자아를 가지고 있습니다.

* **Instruct 모델 튜닝 평가 결과**: 
  * **ROUGE-1**: `0.3821` (Base 대비 ▼ 0.0532 하락)
  * **ROUGE-2**: `0.0922` (Base 대비 ▼ 0.0277 하락)
  * **ROUGE-L**: `0.3486` (Base 대비 ▼ 0.0704 하락)
  * **BLEU 스코어**: `0.1321` (Base 대비 ▼ 0.0920 하락)

* **엔지니어링 고찰 (왜 점수가 떨어졌는가?)**:
  * **정렬 세금 (Alignment Tax) 현상**: 편향이 제거된 이 훌륭한 Instruct 모델은 이미 강력한 '친절한 챗봇' 자아(Persona)를 내부에 학습한 상태입니다. 여기에 우리가 만든 "특수한 작명 마크다운 포맷"을 강제로 주입하려다 보니, 모델 내부의 기존 챗봇 규칙과 우리의 엄격한 작명 규칙이 충돌하는 현상(Catastrophic Interference)이 발생했습니다.
  * **결론 (기술적 성과 및 발표 요약 멘트)**: 
    > "저희는 편향이 제거된 세계 최고 수준의 대화형 모델(Vector Institute)로도 실험을 진행해 보았습니다. 하지만 이미 '친절한 챗봇'이라는 자아가 강하게 학습된 모델에 특수한 작명 포맷을 강제로 주입하려니, 모델 내부에서 충돌이 일어나 성능이 오히려 하락했습니다. 이를 통해 작명처럼 엄격한 폼이 필요한 작업에는 백지상태인 **Base 모델**을 쓰는 것이 기술적으로 옳다는 것을 입증했습니다!"

---

### 💡 [한계점 및 향후 개선 방향 (고찰)]
* 300건의 데이터는 포맷 학습에는 충분했으나, 4B 모델의 범용적 대화 능력을 완벽히 일반화(Generalization)하기에는 부족할 수 있습니다. 
* 따라서 예상치 못한 프롬프트(Unseen Data) 입력 시 과적합(Overfitting)으로 인해 포맷이 무너질 위험이 존재합니다. 
* **향후 계획 (v2.0)**: 데이터 증강 기법을 통해 10,000건 이상의 고품질 대화셋을 구축하고, 모델과 특수 토큰(<|im_start|>)의 충돌 원인을 수정하여 대화에 특화된 Instruct 모델 기반 파인튜닝을 재수행할 예정입니다.
